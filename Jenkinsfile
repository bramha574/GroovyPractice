pipeline {
    agent {
        node {
            label 'master'
            }
        }
stages {
    stage('Node Details') {
        steps {
            println "${NODE_NAME}"
        }
    }


    stage("Test") {
        steps {
            println "${NODE_NAME}"
    }
}
}
}
